import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const WorkoutSummaryPage = ({ navigation, route }) => {
  return (
    <View>
      <Text>Workout End summary page</Text>
    </View>
  );
};

export default WorkoutSummaryPage;
